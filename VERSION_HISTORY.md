# ประวัติการพัฒนาระบบ (Version History)

เอกสารนี้สรุปการพัฒนาระบบ Online Leave Approval System ตั้งแต่ Version 1 ถึง Version 7 โดยอ้างอิงจาก Git history, branch และ tag ที่มีอยู่ใน repository ณ วันที่ 10 กันยายน 2026

## จุดอ้างอิงแต่ละเวอร์ชัน

| เวอร์ชัน | วันที่ | Git reference | Commit |
|---|---:|---|---|
| Version 1 | 10 ส.ค. 2026 | `main` | `7091969` |
| Version 2 | 18 ส.ค. 2026 | `version-2` | `6ee05dd` |
| Version 3 | 24 ส.ค. 2026 | `version-3` | `b20a7c6` |
| Version 4 | 31 ส.ค. 2026 | `version-4` | `79c1e31` |
| Version 5 | 4 ก.ย. 2026 | `version-5` | `3a3b402` |
| Version 6 | 7 ก.ย. 2026 | จุด commit บน `version-6` | `2ad7f67` |
| Version 7 | 9 ก.ย. 2026 | tag `v7` / branch `version-6` | `702bf43` |

> หมายเหตุ: Version 7 มี tag `v7` แต่ยังอยู่บน branch ชื่อ `version-6` เนื่องจากพัฒนาต่อบน branch เดิม ส่วนไฟล์ `CURRENT_TUNNEL_URL.txt` ที่ยังแก้ไขอยู่ใน working tree ไม่รวมอยู่ใน Version 7

## Version 1 — ระบบพื้นฐานที่ใช้งานได้ครบตามบทบาท

เป้าหมายของเวอร์ชันนี้คือสร้างระบบตั้งต้นให้สามารถเข้าสู่ระบบและทำงานตามบทบาทหลักทั้ง 4 บทบาทได้

- วางโครงสร้าง Frontend ด้วย React/Vite และ Backend ด้วย Node.js/Express เชื่อมต่อ MariaDB
- จัดเตรียม Docker Compose สำหรับ Frontend, Backend, Database, phpMyAdmin และ MailHog
- เพิ่มระบบ Authentication, JWT, RBAC และ Protected Route สำหรับ Employee, Supervisor, HR และ Admin
- เพิ่มนโยบายรหัสผ่าน การเปลี่ยนรหัสผ่านครั้งแรก และการรีเซ็ตรหัสผ่านด้วย OTP ทางอีเมล
- เพิ่มการจัดการข้อมูลส่วนตัวและรูปโปรไฟล์
- เพิ่ม flow คำขอลาหลัก ได้แก่ บันทึกแบบร่าง แก้ไข ส่งคำขอ ดูรายละเอียด ยกเลิก อนุมัติ และปฏิเสธ
- เพิ่มการตรวจสิทธิ์วันลา ประเภทการลา วันหยุด และไฟล์แนบ
- เพิ่มระบบแจ้งเตือนระหว่างผู้ยื่นคำขอกับผู้อนุมัติ
- เพิ่มหน้า Dashboard และหน้าหลักแยกตามแต่ละบทบาท
- เพิ่มโมดูล HR สำหรับพนักงาน ประเภทการลา สิทธิ์วันลา วันหยุด และรายงาน
- เพิ่มโมดูล Admin สำหรับบัญชีผู้ใช้ แผนก ตำแหน่ง และประวัติการใช้งาน
- ปรับหน้าจอหลักและข้อความในระบบเป็นภาษาไทย
- เพิ่มชุดทดสอบด้าน Authentication, Password, Profile, HR Management และ Admin User

## Version 2 — เชื่อมข้อมูลจริงและปรับหน้าจอทั้งระบบ

เป้าหมายของเวอร์ชันนี้คือเปลี่ยนหน้าที่เคยพึ่งข้อมูลจำลองให้ใช้ข้อมูลจาก Backend มากขึ้น พร้อมยกระดับความสม่ำเสมอของ UI

- เพิ่ม API รายงานการลา พร้อมตัวกรองสถานะ ประเภทการลา แผนก และช่วงวันที่
- เพิ่ม API สำหรับดึง Audit Log จากฐานข้อมูล
- เชื่อมหน้า Dashboard, รายงาน, การแจ้งเตือน และหน้าจัดการต่าง ๆ กับข้อมูลจริงมากขึ้น
- ปรับหน้า Employee, Supervisor, HR และ Admin ให้ใช้รูปแบบการแสดงผลที่สอดคล้องกัน
- เพิ่มตัวช่วยจัดรูปแบบข้อมูล เช่น วันที่ สถานะ บทบาท และประเภทการลา
- ปรับเส้นทางจาก Notification ให้เปิดหน้ารายละเอียดตามบทบาทเจ้าของคำขอได้ถูกต้อง
- เพิ่ม Application Error Boundary เพื่อรองรับข้อผิดพลาดที่เกิดจากหน้า Frontend
- แก้ transaction ของคำขอลาให้ rollback เมื่อ validation หรือขั้นตอนบันทึกไม่ผ่าน
- กำหนดให้ระบบปฏิเสธการลาครึ่งวันอย่างชัดเจน เนื่องจากยังไม่อยู่ในขอบเขตระบบ
- ปรับหน้าเปลี่ยนรหัสผ่าน ข้อมูลส่วนตัว สิทธิ์วันลา คำขอของฉัน และหน้าจัดการ Master Data
- ปรับ Test Case และ Use Case Diagram ให้ตรงกับระบบในรอบนี้

## Version 3 — สร้างมาตรฐาน UI และแก้ความถูกต้องของ Leave Flow

เป้าหมายของเวอร์ชันนี้คือสร้าง Design System กลาง ลดความแตกต่างระหว่างหน้า และแก้จุดเสี่ยงในกระบวนการลา

- เพิ่ม Design Token, Theme และ Shared UI Components กลางสำหรับใช้ร่วมกันทั้งระบบ
- ปรับ App Shell, Sidebar, Layout, Card, Table, Form, Dialog และ Responsive ให้เป็นมาตรฐานเดียวกัน
- เพิ่มรูปแบบสีตามบทบาท โดยยังรักษารูปแบบการใช้งานหลักให้เหมือนกัน
- เพิ่ม component แสดงเลขคำขอให้มีรูปแบบเดียวกัน
- ปรับหน้า Login, Password Recovery, Dashboard, My Requests, Notification, Profile และ Change Password
- ปรับหน้ารายการรออนุมัติ รายงานทีม และหน้ารายละเอียดคำขอ
- ปรับหน้าจัดการพนักงาน สิทธิ์การลา ประเภทการลา วันหยุด และรายงาน HR
- ปรับหน้า User Management และ Audit Log ของ Admin
- เพิ่มฐานอ้างอิง schema ปัจจุบันและคำอธิบาย schema
- แก้ validation และการจัดการไฟล์แนบของ Leave Request
- เพิ่มชุดทดสอบ Leave Remediation เพื่อป้องกัน regression ใน flow การลา

## Version 4 — ปรับ UX ของ Form, Auth และ Workflow ครั้งใหญ่

เป้าหมายของเวอร์ชันนี้คือทำให้หน้าจอใช้งานจริงได้สะดวกขึ้น ลดหน้าที่กว้างหรือรก และทำให้ผู้ใช้เข้าใจสถานะของระบบได้ชัดเจน

- ปรับ Login, Forgot Password, Verify OTP, Reset Password และ Change Password ใหม่
- เพิ่มองค์ประกอบแสดงสิทธิ์วันลาบน Dashboard ที่ใช้ร่วมกันได้
- ปรับหน้าสร้างคำขอลา รายการคำขอ รายละเอียดคำขอ และการแจ้งเตือน
- ปรับ Profile และ Popup แก้ไขข้อมูลส่วนตัวให้กระชับขึ้น
- ปรับ Form เพิ่ม/แก้ไขพนักงาน ประเภทการลา แผนก ตำแหน่ง และบัญชีผู้ใช้
- ปรับรายการรออนุมัติและหน้าตัดสินใจอนุมัติ/ปฏิเสธ
- ปรับ Dashboard และรายงานของ Employee, Supervisor, HR และ Admin
- เพิ่มหน้า Not Found และหน้าบอกสถานะเมื่อ Backend ไม่พร้อมใช้งาน
- ปรับ Error Handling ของ Frontend API ให้แสดงสถานะระบบได้ชัดเจนขึ้น
- เพิ่มสคริปต์ตรวจและซ่อมข้อมูล Leave Runtime
- ปรับ Docker/Vite และเพิ่มสคริปต์เริ่มระบบสำหรับสภาพแวดล้อมที่ใช้ทดสอบ
- เก็บ Database Backup และปรับเอกสาร Requirement, Workflow และ Test Case

## Version 5 — ปรับ Page Structure, Audit Log และเตรียมข้อมูลสำหรับตรวจระบบ

เป้าหมายของเวอร์ชันนี้คือจัดหน้าทั้งระบบให้เป็นระเบียบ ลดหัวข้อหรือพื้นที่ซ้ำซ้อน และเตรียมระบบกับฐานข้อมูลสำหรับการตรวจของพี่เลี้ยง

- Audit การใช้ Page Header ทุกหน้า และปรับให้เหมาะกับบริบทของ List, Form และ Detail
- รวมรูปแบบ Page Header, Section Title, Back Button และ spacing ให้เป็นมาตรฐานกลาง
- ปรับความกว้างและความสูงของ Form/Card/Popup ที่ใหญ่เกินความจำเป็น
- เปลี่ยนหน้าเพิ่ม/แก้ไขบัญชีผู้ใช้และแผนกบางส่วนให้ทำงานผ่าน Popup
- เพิ่ม Role Card Surface และปรับสีของการ์ดตามบทบาท
- ปรับปุ่มให้ใช้สีตามความหมายของ action เช่น บันทึก แก้ไข ยกเลิก และลบ
- ปรับ Search, Filter, Active Filter, Pagination และข้อความ `หน้า ... จาก ...` ในหน้ารายการ
- ปรับหน้ารายงานการลาและหน้ารายงานทีม รวมถึงการแสดงผู้อนุมัติ
- ปรับการนำทางย้อนกลับให้สัมพันธ์กับหน้าที่ผู้ใช้เข้ามา
- เพิ่มและขยาย Audit Middleware ให้บันทึก action สำคัญของระบบมากขึ้น
- ปรับชื่อ action ระหว่าง Backend และ Frontend ให้ตรงกัน พร้อมเพิ่ม error state เมื่อโหลด Audit Log ไม่สำเร็จ
- เพิ่มหน้ารออนุมัติและรายละเอียดคำขอสำหรับ HR
- ปรับ Auth Guard ไม่ให้แสดงข้อความ “กรุณาเข้าสู่ระบบ” ซ้อนบนหน้า Login ตอนเริ่มระบบ
- ทำรอบ Backup, Clean Initial State, Restore Database และตรวจข้อมูล 4 บัญชีหลัก
- เพิ่ม runtime smoke test สำหรับ Employee, Supervisor, HR และ Admin
- จัดทำ Backup ก่อนทดสอบกับพี่เลี้ยงและเก็บ snapshot ฐานข้อมูลที่ใช้ตรวจสอบย้อนหลัง

## Version 6 — เก็บรายละเอียด UI และทำรหัสพนักงานตามบทบาท

เป้าหมายของเวอร์ชันนี้คือแก้รายละเอียดจากการทดสอบใช้งานจริงและลดความสับสนของหน้าจัดการต่าง ๆ

- ปรับ Search/Filter และการล้างตัวกรองให้มี behavior สม่ำเสมอขึ้น
- ปรับตาราง การ์ด สัดส่วนหน้า spacing และ alignment ของทุกบทบาท
- ปรับหน้าเพิ่ม/แก้ไขประเภทการลา พนักงาน แผนก ตำแหน่ง และบัญชีผู้ใช้
- ปรับหน้ารายงานการลา รายงานทีม รายการรออนุมัติ และ Audit Log
- ปรับคำอธิบายและข้อความที่ไม่จำเป็นออกจากหน้ารายการ
- ปรับ Pagination และข้อความภาษาไทยของวันที่/จำนวนรายการ
- เพิ่มการสร้างรหัสพนักงานอัตโนมัติตามบทบาท ได้แก่ `EMP`, `SUP`, `HR` และ `ADM`
- ปรับรหัสพนักงานให้สอดคล้องกับบทบาทเมื่อ Admin สร้างหรือแก้ไขบัญชี
- ใช้ transaction ในการสร้าง/แก้ไขบัญชี เพื่อไม่ให้ข้อมูลพนักงานและบัญชีไม่ตรงกัน
- ปรับข้อความ Validation และ Error ในหน้า Form/Popup ให้บอกสาเหตุที่บันทึกไม่ได้
- ปรับรายการนโยบายรหัสผ่านและหน้ากู้คืนรหัสผ่านให้กระชับขึ้น
- เพิ่มและปรับชุดทดสอบ Admin User และ HR Management ให้ครอบคลุม behavior ใหม่

## Version 7 — เพิ่มกฎธุรกิจจริง การลบข้อมูลอย่างปลอดภัย และ Notification Flow

เป้าหมายของเวอร์ชันนี้คือทำให้ระบบพร้อมใช้งานใกล้เคียงระบบจริงมากขึ้น โดยเน้นกฎวันลา ความถูกต้องของสิทธิ์ การจัดการบัญชี และการตรวจสอบย้อนหลัง

### Leave Request และ Leave Entitlement

- กำหนดการลาทั่วไปให้ยื่นล่วงหน้าอย่างน้อย 3 วัน
- แยกข้อยกเว้นลาป่วยให้สามารถยื่นวันปัจจุบันหรือย้อนหลังได้
- ตรวจวันเริ่มต้นตามประเภทการลาทั้ง Frontend และ Backend
- ป้องกันการส่งคำขอเกินสิทธิ์ โดยนับวันของคำขอที่รออนุมัติร่วมด้วย
- หัก `used_days` เมื่ออนุมัติแล้วเท่านั้น และคืน/คำนวณยอดให้ถูกต้องเมื่อสถานะเปลี่ยน
- สร้างและปรับสิทธิ์การลาปีปัจจุบันอัตโนมัติจากค่าที่กำหนดในประเภทการลา
- ปิดการเพิ่มหรือแก้สิทธิ์รายบุคคล เมื่อระบบใช้โควตากลางจากประเภทการลา
- ปรับหน้ารายการสิทธิ์ให้รวมข้อมูลพนักงานและเปิดดูรายละเอียดสิทธิ์แต่ละประเภทได้จากแถว
- ปรับข้อความ validation ของแบบร่างและการส่งคำขอเป็นภาษาไทย พร้อมระบุ field ที่ขาด

### HR Management

- สร้างรหัสประเภทการลาอัตโนมัติ
- กำหนดจำนวนวันลาเริ่มต้นให้มากกว่า 0
- เพิ่มการลบประเภทการลาหลังปิดใช้งาน โดยลบสิทธิ์ที่ยังไม่ถูกใช้และป้องกันการลบเมื่อมีประวัติจริง
- เพิ่มการลบพนักงานหลังปิดใช้งาน โดยตรวจบัญชีผู้ใช้ ประวัติคำขอ และผู้ใต้บังคับบัญชาก่อน
- ลบ Leave Entitlement ที่สร้างอัตโนมัติพร้อมพนักงานได้ หากไม่มีประวัติคำขอลา
- เพิ่มการลบแผนกและตำแหน่งหลังปิดใช้งาน พร้อมป้องกันการลบเมื่อยังมีพนักงานอ้างอิง
- ปรับ Popup ให้แสดงสาเหตุเมื่อเพิ่ม แก้ไข หรือลบข้อมูลไม่สำเร็จ

### Admin และ Account Lifecycle

- เพิ่มการลบบัญชีผู้ใช้หลังปิดใช้งาน พร้อมตรวจประวัติการอนุมัติและข้อมูลอ้างอิงก่อนลบ
- ป้องกัน Admin ลบบัญชีที่กำลังเข้าสู่ระบบอยู่
- เพิ่ม Login Lockout เมื่อกรอกรหัสผ่านผิดตามจำนวนครั้งที่กำหนด และบันทึกสถานะการล็อกในฐานข้อมูล
- ปรับการสร้างบัญชีให้แสดง Username และรหัสผ่านชั่วคราวครั้งเดียวหลังสร้างสำเร็จ
- เพิ่มปุ่มคัดลอกข้อมูลเข้าสู่ระบบสำหรับส่งต่อให้พนักงานใหม่
- บังคับเปลี่ยนรหัสผ่านในการเข้าสู่ระบบครั้งแรก

### Notification และ Audit Log

- เมื่อ HR เพิ่มพนักงาน ระบบแจ้ง Admin ว่ามีพนักงานใหม่รอสร้างบัญชี
- เมื่อ Admin ลบบัญชี ระบบแจ้ง HR เพื่อให้ดำเนินการกับ Employee Record ต่อได้
- เพิ่มเส้นทางจาก Notification ไปหน้าจัดการที่เกี่ยวข้อง
- เพิ่มการ refresh Notification เมื่อเปิดกระดิ่ง กลับมา focus ที่หน้าต่าง และตามรอบเวลา
- เพิ่ม Audit Log สำหรับ action สำคัญ รวมถึงการลบข้อมูลและการเข้าสู่ระบบไม่สำเร็จ
- แยกหมวดและปรับสีของกิจกรรมใน Audit Log ตามผลสำเร็จ/ไม่สำเร็จและประเภทกิจกรรม

### UI, Docker และการทดสอบ

- ปรับการ์ด Dashboard และ Summary Card ของแต่ละบทบาทให้สื่อความหมายถูกต้อง
- ปรับปุ่มลบ/ปิดใช้งาน ตาราง รายงาน Form และ Dialog ให้มีรูปแบบสม่ำเสมอขึ้น
- ปรับหน้าเพิ่ม/แก้ไขบัญชี พนักงาน ประเภทการลา และรายการสิทธิ์ให้ใช้งานจริงได้
- แก้ Docker named volume สำหรับการ clone ไปใช้งานบนเครื่องใหม่
- ปรับขั้นตอนเริ่มระบบใน README และกำหนดการ import database snapshot เมื่อ volume ยังว่าง
- เพิ่ม migration สำหรับ Login Lockout
- เพิ่มและปรับการทดสอบ Login Lockout, Admin User, HR Management, Leave Remediation และ Audit Activity

## สถานะปัจจุบัน

- เวอร์ชันล่าสุดใน Git คือ Version 7 ที่ tag `v7`
- Source code ของ Version 7 อยู่ที่ commit `702bf43`
- Branch ที่ใช้งานอยู่คือ `version-6` และชี้ไปที่ commit เดียวกับ tag `v7`
- ระบบมีประวัติการพัฒนาแบบต่อเนื่อง โดยแต่ละเวอร์ชันรวมการแก้ไขของเวอร์ชันก่อนหน้าไว้แล้ว

